import { BlogHighlightDirective } from './blog-highlight.directive';

describe('BlogHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
